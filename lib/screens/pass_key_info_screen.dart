import 'package:flutter/material.dart';

class PassKeyInfoScreen extends StatelessWidget {
  const PassKeyInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
